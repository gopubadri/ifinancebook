# iFinance Books — Frontend

React + Vite UI for iFinance Books.

## Run

From repo root:

```bash
npm run dev:frontend
```

Or:

```bash
cd frontend
npm install
npm run dev
```

Opens at http://localhost:5173 (proxies `/api` → backend `:4000`).

Requires the backend running (`npm run dev:backend`).
